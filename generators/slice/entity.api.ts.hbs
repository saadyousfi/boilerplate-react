/* eslint-disable require-yield */
import { Update } from "@reduxjs/toolkit"
import { {{TypeName}}, {{TypeName}}Creator } from "types";

  export function* fetchMany(){
    return []
  }
  export function* updateOne(update: Update<{{TypeName}}>){}
  export function* deleteOne(id: string){}
  export function* createOne(payload: {{TypeName}}Creator) {
    return {}
  }